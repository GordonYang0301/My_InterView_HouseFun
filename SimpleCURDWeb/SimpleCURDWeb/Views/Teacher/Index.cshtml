@model IEnumerable<SimpleCURDWeb.Models.老師資料表>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.老師姓名)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.研究領域)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.老師姓名)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.研究領域)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.老師編號 }) |
            @Html.ActionLink("Details", "Details", new { id=item.老師編號 }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.老師編號 })
        </td>
    </tr>
}

</table>
